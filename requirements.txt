# AMLGuard — Phase 1 dependencies (academic delivery: notebooks/01_aml_pipeline.ipynb)
pandas>=2.0
numpy>=1.24
matplotlib>=3.7
scikit-learn>=1.3
imbalanced-learn>=0.11
xgboost>=2.0
shap>=0.44
gdown>=5.0          # runtime download of HI-Small_Trans.csv (see notebook Section 2)

# --- Phase 2 (serving layer) — uncomment when implementing src/api.py ---
# fastapi
# uvicorn[standard]
# streamlit
# joblib
